{
  "name": "vite-preact-ts-starter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "dotnet tool restore && dotnet fable watch src --sourceMaps --runFast vite",
    "build": "dotnet tool restore && dotnet fable src --run tsc -b && vite build",
    "preview": "vite preview",
    "clean": "dotnet fable clean src --yes"
  },
  "dependencies": {
    "preact": "^10.24.1"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.1",
    "typescript": "^5.6.2",
    "vite": "^5.4.8"
  }
}
